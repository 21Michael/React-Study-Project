const initialState = {
    hidden: false,
    path: '/orderSent'
}

export default function orderSentReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}